# Lpedia Frontend

九龍婦女福利會李炳紀念學校 AI學習平台前端界面

## 技術架構

- **React 19.2** + **TypeScript**
- **Vite 6** 建置工具
- **Tailwind CSS** 樣式框架
- **React Router 7** 路由管理
- **Lucide React** 圖標庫

## 開發命令

```bash
# 安裝依賴
npm install

# 開發模式 (localhost:3000)
npm run dev

# 生產建置
npm run build

# 預覽建置結果
npm run preview
```

## 環境設定

創建 `.env.local` 檔案：

```
VITE_API_BASE_URL=http://localhost:5000/api
VITE_TLDRAW_LICENSE_KEY=your_tldraw_license_key
```

## 專案架構

### 核心功能
- **雙入口登入**: 教師和學生分別登入
- **教師儀表板**: 任務分發、學生管理
- **學生儀表板**: 任務接收、進度追蹤
- **動漫風格設計**: 適合小學生的視覺介面

### 路由系統
- `/` - 登入頁面
- `/teacher` - 教師儀表板
- `/student` - 學生儀表板

### 組件結構

```
components/
├── Button.tsx         # 按鈕組件
├── Input.tsx          # 輸入框組件
└── Select.tsx         # 下拉選單組件

pages/
├── LoginPage.tsx      # 登入頁面
├── TeacherDashboard.tsx  # 教師儀表板
└── StudentDashboard.tsx  # 學生儀表板
```

### 設計系統

#### 品牌色彩
```css
--brand-green-light: #A1D9AE    /* 學生介面主色 */
--brand-green-dark: #5E8B66     /* 學生介面重點色 */
--brand-yellow: #F4D03F         /* 教師介面主色 */
--brand-yellow-light: #FDEEAD   /* 教師卡片背景 */
--brand-brown: #5E4C40          /* 文字和邊框 */
```

#### 學科配色
- **語文**: 粉紅色 `#F8C5C5` 📖
- **數學**: 藍色 `#B5D8F8` 🧮
- **英文**: 黃色 `#F8E2B5` 🔤
- **科學**: 綠色 `#B5F8CE` 🔬
- **藝術**: 粉色 `#F8B5E0` 🎨

## Railway 部署

使用 `railway.json` 和 `nixpacks.toml` 配置自動部署到 Railway。

### 環境變數設定
```
NODE_ENV=production
VITE_API_BASE_URL=https://lpedianew-backend-production.up.railway.app/api
VITE_TLDRAW_LICENSE_KEY=your_tldraw_license_key
```

## 開發指南

### 添加新頁面
1. 在 `pages/` 創建組件
2. 在 `App.tsx` 添加路由
3. 更新 `types.ts` 如需新的介面定義

### 樣式規範
- 使用 Tailwind CSS 工具類
- 遵循動漫風格設計（圓角、粗邊框、陰影）
- 響應式設計：手機優先，`lg:` 斷點用於桌面版

### 狀態管理
- 使用 React Hooks (`useState`, `useEffect`)
- 路由導航使用 `useNavigate`
- 表單狀態本地管理

## 瀏覽器支援

- Chrome 90+
- Firefox 90+
- Safari 14+
- Edge 90+

## 部署注意事項

1. **建置前確認**: API URL 正確設定
2. **靜態資源**: 確保圖片路徑正確
3. **路由模式**: 使用 HashRouter 相容性更佳

---

**九龍婦女福利會李炳紀念學校**
**校本AI學習平台開發團隊**
