import { Subject } from './types';

export const VISIBLE_SUBJECTS: Subject[] = [Subject.SCIENCE];
export const DEFAULT_SUBJECT: Subject = Subject.SCIENCE;
export const GROUPS_ENABLED = false;
export const SINGLE_SUBJECT_MODE = VISIBLE_SUBJECTS.length === 1;

